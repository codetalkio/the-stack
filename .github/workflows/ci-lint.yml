name: "CI: Lint"

on:
  workflow_dispatch:
  push:
    paths:

defaults:
  run:
    shell: bash # Set the default shell to bash.

jobs:
  lint:
    strategy:
      fail-fast: false
      matrix:
        service:
          - { name: ms-router, toolchain: stable, with-openssl: true, with-protobuf: true }
          - { name: ms-queue, toolchain: stable, with-openssl: false, with-protobuf: false }
          - { name: ms-gql-todo, toolchain: stable, with-openssl: false, with-protobuf: false }
          - { name: ms-gql-media, toolchain: stable, with-openssl: false, with-protobuf: false }
          - { name: ui-app, toolchain: nightly, with-openssl: false, with-protobuf: false }
    uses: ./.github/workflows/wf-lint.yml
    with:
      working-directory: ${{ matrix.service.name }}
      toolchain: ${{ matrix.service.toolchain }}
      with-openssl: ${{ matrix.service.with-openssl }}
      with-protobuf: ${{ matrix.service.with-protobuf }}
