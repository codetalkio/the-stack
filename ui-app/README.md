# UI: Application

Setup:

```bash
rustup toolchain install nightly
rustup default nightly
rustup target add wasm32-unknown-unknown
```

Build:

```bash
$ trunk build
```

Development:

```bash
$ trunk serve
```
