{
  "name": "ms-gateway",
  "module": "src/lambda.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/lambda.ts",
    "build": "bun build ./src/lambda.ts --target=node --minify --outdir ./dist",
    "dev": "bun run --hot src/local.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@apollo/gateway": "^2.5.6",
    "@apollo/server": "^4.9.4",
    "@as-integrations/aws-lambda": "^3.1.0",
    "graphql": "^16.8.1"
  }
}
